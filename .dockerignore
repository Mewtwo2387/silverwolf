node_modules
.env
.git
npm-debug.log
logs.txt
database.db
persistence/
.DS_Store
local_db_folder/
# Built inside the Dockerfile from input.css / app.src.js — never copy a stale
# local artifact into the image.
site_src/Assets/styles.css
site_src/Assets/app.js
# Downloaded (checksum-verified) inside the Dockerfile by scripts/fetch-soundfont.ts
# — never copy a stale local soundfont into the image.
data/soundfonts/
# Android app — client-side only, never part of the server image
android/
*.apk